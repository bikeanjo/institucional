# Environment Configuration Example
# Copy this file to .env.stage or .env.prod and configure accordingly

ENV=stage
FRONTEND_PORT=80
API_PORT=3000
FRONTEND_BIND=0.0.0.0
API_BIND=0.0.0.0

# Deploy location
DEPLOY_LOCAL=true
# DEPLOY_HOST=your-ssh-host
# DEPLOY_PATH=/path/to/deploy

# Domain configuration (production only)
# DOMAIN_PRIMARY=your-domain.com
# SSL_EMAIL=your-email@example.com
