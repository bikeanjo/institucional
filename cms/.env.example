# CMS Configuration

# Porta do servidor (mudei para 3002 porque 3001 está sendo usado pelo Nginx)
PORT=3002

# Senha de administração (mudar em produção!)
ADMIN_PASSWORD=admin123

# Caminho dos arquivos JSON (relativo ou absoluto)
CONTENT_DIR=../public/data

# Secret para JWT (gerar uma string aleatória em produção)
JWT_SECRET=change-me-in-production
